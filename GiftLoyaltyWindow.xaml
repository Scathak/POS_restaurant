<Window x:Class="POS_restaurant.GiftLoyaltyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:POS_restaurant"
        mc:Ignorable="d"
        Title="GiftLoyaltyWindow" 
        WindowState="Maximized"
        WindowStyle="None">
    <Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top">
            <Button Content="Gifts" Width="100" Height="50" Margin="10"/>
            <Button Content="Gift cards" Width="100" Height="50" Margin="10"/>
            <Button Content="Discounts" Width="100" Height="50" Margin="10"/>
            <Button Content="Discount Codes" Width="auto" Height="50" Margin="10"/>
        </StackPanel>
    </Grid>
</Window>
